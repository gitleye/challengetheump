# Core data pipeline dependencies
# Pinned to ensure reproducible builds.
# Note: Python 3.12 is used in CI (GitHub Actions); 3.14 is supported locally.

pandas>=3.0.3,<4.0
numpy>=2.4.5,<3.0
requests>=2.31,<3.0
pydantic>=2.13.4,<3.0
structlog>=25.5.0,<26.0

# Baseball data
pybaseball>=2.2.7

# HTTP
urllib3>=2.0,<3.0

# Development / testing
pytest>=8.0
pytest-cov>=7.1.0
